import 'package:flutter/material.dart';

class JobListingItem extends StatelessWidget {
  const JobListingItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
